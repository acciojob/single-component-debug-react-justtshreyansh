import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

// Render the App component to the #root DOM node
ReactDOM.render(<App />, document.getElementById("root"));
